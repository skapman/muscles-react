# 🎯 Финальные задачи проекта Fascia

## ✅ Что уже сделано

- ✅ MDX Infrastructure (Phases 1-2)
- ✅ Build Scripts с валидацией
- ✅ Content Index Hook
- ✅ Article Rendering System
- ✅ **Миграция данных завершена:** 10 мышц, 5 целей, 20 упражнений

## 📊 Текущее состояние

**Content Index:** 35 записей
- 10 muscles
- 20 exercises
- 5 goals
- 0 pain (не было в данных)

---

## 🔧 Что осталось сделать

### 1. Дебаг графа (приоритет: высокий)

**Проблема:** Граф работает с багами при одном узле, нужно протестировать с полным датасетом.

**Что проверить:**
```bash
# Откройте граф
http://localhost:5173/graph
```

**Ожидаемое поведение:**
- ✅ Отображаются все 35 узлов
- ✅ Узлы разных цветов по типам (muscles/exercises/goals)
- ✅ Связи между узлами видны
- ✅ Hover на узле подсвечивает связанные
- ✅ Клик на узел работает
- ✅ Drag & drop узлов работает
- ✅ Zoom/pan работает

**Возможные проблемы:**

1. **Узлы не отображаются**
   - Проверьте консоль браузера
   - Проверьте что `buildGraphData()` возвращает данные
   - Проверьте `useGraphData.js` - правильно ли читает индекс

2. **Связи не рисуются**
   - Проверьте формат links в `buildGraphData()`
   - Должно быть: `{ source: "id1", target: "id2" }`

3. **Цвета узлов неправильные**
   - Проверьте `getNodeColor()` в GraphSVG.jsx
   - Типы должны быть: "muscle", "exercise", "goal", "pain"

4. **Производительность**
   - 35 узлов - это нормально
   - Если тормозит - можно добавить фильтры

**Файлы для проверки:**
- [`src/hooks/useContentIndex.js`](src/hooks/useContentIndex.js:88) - функция `buildGraphData()`
- [`src/hooks/useGraphData.js`](src/hooks/useGraphData.js:1) - использование buildGraphData
- [`src/components/graph/GraphSVG.jsx`](src/components/graph/GraphSVG.jsx:1) - рендеринг

---

### 2. Тестирование статей (приоритет: средний)

**Проверить несколько статей каждого типа:**

```bash
# Мышцы
http://localhost:5173/muscles/pectoralis-major
http://localhost:5173/muscles/rectus-abdominis
http://localhost:5173/muscles/trapezius-upper

# Упражнения
http://localhost:5173/exercises/bench-press
http://localhost:5173/exercises/pull-ups
http://localhost:5173/exercises/squats

# Цели
http://localhost:5173/goals/bench-100kg
http://localhost:5173/goals/six-pack-abs
```

**Что проверить:**
- ✅ Статья загружается
- ✅ Заголовок и теги отображаются
- ✅ MDX контент рендерится
- ✅ Панель "Связанные материалы" показывает связи
- ✅ Клики по связанным материалам работают
- ✅ Mobile версия работает

---

### 3. Обновить документацию (приоритет: низкий)

**Обновить README.md:**

Добавить секцию о контентном пайплайне:

```markdown
## 📝 Content Management

### Adding New Content

1. Create MDX file in `content/{type}/` folder
2. Add frontmatter with metadata
3. Run `npm run build:index` to update index
4. Run `npm run validate` to check
5. Test in browser

### Content Structure

- `content/muscles/` - muscle articles
- `content/exercises/` - exercise guides
- `content/goals/` - fitness goals
- `content/pain/` - pain & injury info (future)

### Build Scripts

- `npm run build:index` - Generate content index
- `npm run validate` - Validate content
- `npm run dev` - Dev server (auto-builds index)
- `npm run build` - Production build (with validation)
```

---

### 4. Пометить старые модули как deprecated (приоритет: низкий)

**Добавить комментарии в старые файлы:**

```javascript
// src/data/muscleData.js
/**
 * @deprecated
 * This file is deprecated. Content has been migrated to MDX files in /content/muscles/
 * Use content-index.json and MDX files instead.
 *
 * Migration completed: 2026-02-15
 * See: /content/muscles/*.mdx
 */
export const muscleData = { ... }
```

То же для:
- `src/data/goalData.js`
- `src/data/exerciseData.js`

**НЕ УДАЛЯТЬ** эти файлы - они могут использоваться в старом коде.

---

### 5. Опциональные улучшения (если есть время)

#### 5.1 Поиск по тегам
Сделать теги кликабельными - фильтровать контент по тегу.

#### 5.2 Breadcrumbs
Добавить навигацию: Home > Muscles > Pectoralis Major

#### 5.3 Изображения
Добавить реальные изображения в `public/content-images/`

#### 5.4 SEO
Добавить meta tags для статей (title, description, og:image)

#### 5.5 Связи между статьями
Сейчас related указаны вручную. Можно добавить автоматические связи на основе тегов.

---

## 🧪 Чеклист перед коммитом

- [ ] `npm run build:index` - успешно
- [ ] `npm run validate` - только warnings (не errors)
- [ ] Граф отображает все узлы
- [ ] 5+ статей открываются корректно
- [ ] Связанные материалы работают
- [ ] Mobile версия работает
- [ ] README.md обновлён
- [ ] Старые модули помечены @deprecated

---

## 🚀 Deployment Checklist

Перед деплоем на production:

1. **Build test:**
   ```bash
   npm run build
   npm run preview
   ```

2. **Check bundle size:**
   - MDX файлы не должны попасть в bundle
   - Только content-index.json (~50-100KB)

3. **Test production build:**
   - Все статьи работают
   - Граф работает
   - Нет console errors

4. **Environment:**
   - Убедитесь что `npm run build:index` запускается в CI/CD
   - content-index.json должен генерироваться при каждом build

---

## 📈 Метрики успеха

**Контент:**
- ✅ 35 статей мигрировано
- ✅ Автоматическая индексация работает
- ✅ Валидация работает

**UX:**
- ✅ Статьи загружаются < 1 сек
- ✅ Граф интерактивный
- ✅ Связи между контентом работают
- ✅ Mobile-friendly

**DX (Developer Experience):**
- ✅ Добавление контента = 1 MDX файл
- ✅ Автоматическая валидация
- ✅ Автоматические обратные связи
- ✅ Понятная структура

---

## 🎉 Готово!

После выполнения этих задач проект полностью готов к использованию.

**Основной фокус:** Дебаг графа - это единственная критичная задача.

Остальное - опционально и можно делать постепенно.
